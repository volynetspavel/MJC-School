plugins {
    id 'java'
    id 'war'
}

group 'com.epam.esm'

dependencies {
    implementation project (':core')

    providedCompile group: 'javax.servlet', name: 'javax.servlet-api', version: '4.0.1'
}